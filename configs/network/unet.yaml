# UNet1D network configuration (aligned with ChiUNet from much-ado-about-noising)

name: unet
network_type: unet

# Architecture parameters
model_dim: 256
emb_dim: 256
dim_mult: [1, 2, 4]
kernel_size: 5
n_groups: 8

# FiLM conditioning
cond_predict_scale: true
