# MLP network configuration

name: mlp
network_type: mlp

# Architecture parameters (residual block MLP)
emb_dim: 512
n_blocks: 6
expansion_factor: 4
dropout: 0.1

# Time embedding (uniform frequency Fourier)
timestep_emb_dim: 128
max_freq: 100.0

# Activation function
activation: gelu
